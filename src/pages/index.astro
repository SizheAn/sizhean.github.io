---
import BaseLayout from '../layouts/BaseLayout.astro';
import FocusCard from '../components/FocusCard.astro';
import Hero from '../components/Hero.astro';
import PublicationCard from '../components/PublicationCard.astro';
import ThemeToggle from '../components/ThemeToggle.astro';
import { profile } from '../data/profile';
import { featuredWork, otherPublications } from '../data/publications';
---

<BaseLayout>
  <header class="topbar">
    <a href="#top" class="wordmark">{profile.name}</a>
    <nav aria-label="Site navigation">
      <a href="#focus">Focus</a>
      <a href="#work">Work</a>
      <a href="#more">Publications</a>
      <a href={`mailto:${profile.email}`}>Contact</a>
      <ThemeToggle />
    </nav>
  </header>

  <main>
    <Hero />

    <section class="section" id="focus" aria-labelledby="focus-heading">
      <div class="section__header">
        <p class="section__kicker">Current Focus</p>
        <h2 id="focus-heading">Research directions I am working on now.</h2>
      </div>
      <div class="focus-grid">
        {profile.currentFocus.map((area, index) => <FocusCard {...area} index={index} />)}
      </div>
    </section>

    <section class="section" id="work" aria-labelledby="work-heading">
      <div class="section__header">
        <p class="section__kicker">Selected Work</p>
        <h2 id="work-heading">Ordered by how close it sits to a body that acts.</h2>
      </div>
      <div class="work-grid">
        {featuredWork.map((publication) => <PublicationCard publication={publication} />)}
      </div>
    </section>

    <section class="section" id="more" aria-labelledby="more-heading">
      <div class="section__header">
        <p class="section__kicker">Also</p>
        <h2 id="more-heading">Earlier public work.</h2>
      </div>
      <ul class="pub-list">
        {
          otherPublications.map((publication) => (
            <li>
              <span class="pill">{publication.venue} {publication.year}</span>
              <div>
                <p class="pub-list__title">{publication.title}</p>
                <p class="pub-list__authors">{publication.authors}</p>
              </div>
              <div class="link-row link-row--tight">
                {publication.links.map((link) => (
                  <a class="chip chip--sm" href={link.href}>
                    {link.label}
                  </a>
                ))}
              </div>
            </li>
          ))
        }
      </ul>
    </section>

    <section class="section section--bio" aria-labelledby="bio-heading">
      <p class="section__kicker" id="bio-heading">About</p>
      <p>{profile.biography}</p>
    </section>
  </main>

  <footer class="footer">
    <p>{profile.name} · {profile.affiliation} · {profile.location}</p>
    <div class="link-row link-row--tight">
      {
        profile.links
          .filter((link) => !link.pending)
          .map((link) => (
            <a class="chip chip--sm" href={link.href}>
              {link.label}
            </a>
          ))
      }
    </div>
  </footer>
</BaseLayout>
